const Product = require('../models/product.model');

describe('Product model', () => {
  it('should create a new product', async => {
    // Write the test logic to create a new product and verify if it's stored correctly
  });

  it('should a product by ID', async () => {
    // Write test logic to retrieve a product and verify if it's retrieved correctly
  });

  // Write more tests updating and deleting products
});